---
interface Props {
  text?: string;
  class?: string;
}

const {
  text = "50 founding member spots — join the lottery",
  class: className = "",
} = Astro.props;
---

<div
  class:list={[
    "rounded-lg border border-primary/20 bg-primary/10 px-4 py-3 text-center text-body-sm font-medium text-primary-text",
    className,
  ]}
  role="status"
>
  {text}
</div>
