---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Section from "@/components/layout/Section.astro";
import Card from "@/components/ui/Card.astro";
import { t } from "@/i18n/utils";

const locale = "de";
---

<BaseLayout
  title={`${t("nav.accuracy", locale)} — ${t("site.title", locale)}`}
  description={t("site.description", locale)}
  locale={locale}
  path="/accuracy/"
>
  <Section class="py-24 md:py-32">
    <div class="container">
      <h1 class="text-display font-bold tracking-tight mb-6">
        {t("nav.accuracy", locale)}
      </h1>
      <p class="text-body-lg text-muted-foreground max-w-3xl mb-12">
        Vertrauen wird durch Transparenz verdient. Unsere Genauigkeitsmetriken
        werden in Echtzeit über Tausende belgischer Steueranfragen gemessen und
        gewährleisten jedes Mal zuverlässige, quellenbasierte Antworten.
      </p>

      <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
        <Card>
          <h2 class="text-display-sm font-bold mb-2">95,2%</h2>
          <h3 class="text-body font-medium mb-2">Gesamtgenauigkeit</h3>
          <p class="text-body-sm text-muted-foreground">
            Überprüft anhand offizieller belgischer Steuerdokumentation und
            Expertenprüfung.
          </p>
        </Card>

        <Card>
          <h2 class="text-display-sm font-bold mb-2">12.400+</h2>
          <h3 class="text-body font-medium mb-2">Quellenabdeckung</h3>
          <p class="text-body-sm text-muted-foreground">
            Indexierte offizielle Quellen einschließlich Steuergesetzbuch,
            Vorabentscheidungen und Rundschreiben.
          </p>
        </Card>

        <Card>
          <h2 class="text-display-sm font-bold mb-2">&lt;3s</h2>
          <h3 class="text-body font-medium mb-2">Antwortzeit</h3>
          <p class="text-body-sm text-muted-foreground">
            Durchschnittliche Antwortzeit für Anfragen mit vollständigen Zitaten
            und Quellenlinks.
          </p>
        </Card>

        <Card>
          <h2 class="text-display-sm font-bold mb-2">99,1%</h2>
          <h3 class="text-body font-medium mb-2">Zitierrate</h3>
          <p class="text-body-sm text-muted-foreground">
            Jede Antwort durch überprüfbare Verweise auf offizielle Quellen
            belegt.
          </p>
        </Card>
      </div>
    </div>
  </Section>

  <Section muted class="py-24 md:py-32">
    <div class="container">
      <div class="max-w-2xl mx-auto text-center">
        <h2 class="text-h2 font-bold mb-6">
          Treten Sie der Warteliste bei
        </h2>
        <p class="text-body-lg text-muted-foreground mb-8">
          Gehören Sie zu den Ersten, die KI-gestützte belgische Steuerrecherche
          mit vertrauenswürdiger Genauigkeit erleben.
        </p>
        <a
          href={`/${locale}/waitlist`}
          class="inline-flex h-11 min-h-[44px] items-center justify-center rounded-lg bg-primary px-6 text-body-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
        >
          Zur Warteliste
        </a>
      </div>
    </div>
  </Section>
</BaseLayout>
