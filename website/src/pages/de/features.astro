---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Section from "@/components/layout/Section.astro";
import Card from "@/components/ui/Card.astro";
import ScreenshotPlaceholder from "@/components/ui/ScreenshotPlaceholder.astro";
import { t } from "@/i18n/utils";

const locale = "de";
---

<BaseLayout
  title={`${t("nav.features", locale)} — ${t("site.title", locale)}`}
  description={t("site.description", locale)}
  locale={locale}
  path="/features/"
>
  <main id="main-content">
    <Section>
      <div class="max-w-3xl">
        <p class="text-body-sm font-medium text-primary uppercase tracking-wider mb-4">Funktionen</p>
        <h1 class="text-display font-bold tracking-tight mb-6">
          Recherche-Tools für Steuerfachleute, nicht für allgemeines Raten.
        </h1>
        <p class="text-body-lg text-muted-foreground">
          Drei Säulen, die Auryth auszeichnen: messbare Vertrauenswürdigkeit, nachvollziehbare Argumentation und belgische Steuertiefe, die keine allgemeine KI erreichen kann.
        </p>
      </div>
    </Section>

    <Section muted id="honest-by-design">
      <div class="max-w-3xl mb-8">
        <h2 class="text-h2 font-bold text-foreground">{t("trust.transparency", locale)}</h2>
        <p class="mt-4 text-body text-muted-foreground">{t("trust.transparency_desc", locale)}</p>
      </div>
      <div class="grid gap-6 sm:grid-cols-2 max-w-4xl">
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Vertrauenswerte</h3>
          <p class="text-body-sm text-muted-foreground">Jede Antwort auf Zuverlässigkeit bewertet. Hohes, mittleres oder niedriges Vertrauen — immer sichtbar, damit Sie genau wissen, wie viel Gewicht Sie ihr beimessen sollten.</p>
        </Card>
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Quellenangaben</h3>
          <p class="text-body-sm text-muted-foreground">Jede Aussage mit ihrer Rechtsquelle verknüpft. Artikelverweise, Urteile und Rundschreiben — immer überprüfbar, nie eine Black Box.</p>
        </Card>
      </div>
      <div class="mt-10 max-w-4xl">
        <ScreenshotPlaceholder label="Screenshot: Confidence score UI" suggestion="Annotated UI: 0-100 confidence meter with green/amber/red zones, source count badge, list of cited authorities with legislation/ruling icons, and temporal validity indicator." />
      </div>
    </Section>

    <Section id="no-black-box">
      <div class="max-w-3xl mb-8">
        <h2 class="text-h2 font-bold text-foreground">{t("trust.precision", locale)}</h2>
        <p class="mt-4 text-body text-muted-foreground">{t("trust.precision_desc", locale)}</p>
      </div>
      <div class="grid gap-6 sm:grid-cols-2 max-w-4xl">
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Argumentationskette</h3>
          <p class="text-body-sm text-muted-foreground">Folgen Sie der Logik der KI Schritt für Schritt. Sehen Sie, welche Quellen konsultiert wurden, wie sie zusammenhängen und warum die Antwort erreicht wurde.</p>
        </Card>
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Offene Methodik</h3>
          <p class="text-body-sm text-muted-foreground">Unser Retrievalsystem, Scoring-Modell und Quellenhierarchie sind dokumentiert. Keine proprietäre Magie — nur rigorose Technik, die Sie prüfen können.</p>
          <a href={`/${locale}/how-it-works/`} class="mt-3 inline-block text-body-sm font-medium text-primary hover:underline">Sehen Sie, wie es funktioniert &rarr;</a>
        </Card>
      </div>
      <div class="mt-10 max-w-4xl">
        <ScreenshotPlaceholder label="Screenshot: Source panel" suggestion="Expanded source panel: article excerpt with highlighted relevant passage in yellow, publication date, authority tier badge (e.g. 'Tier 2 - Legislation'), and 'Open original' link." />
      </div>
    </Section>

    <Section muted id="jurisdiction-deep">
      <div class="max-w-3xl mb-8">
        <h2 class="text-h2 font-bold text-foreground">{t("trust.belgium", locale)}</h2>
        <p class="mt-4 text-body text-muted-foreground">{t("trust.belgium_desc", locale)}</p>
      </div>
      <div class="grid gap-6 sm:grid-cols-2 max-w-4xl">
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Belgische Quellenhierarchie</h3>
          <p class="text-body-sm text-muted-foreground">Versteht die belgische Rechtshierarchie: Bundesgesetz, Regionaldekrete, Rundschreiben, Rulings und Verwaltungskommentare.</p>
          <a href={`/${locale}/how-it-works/#authority-hierarchy`} class="mt-3 inline-block text-body-sm font-medium text-primary hover:underline">Sehen Sie, wie wir Quellen bewerten &rarr;</a>
        </Card>
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Dreisprachig NL/FR/EN</h3>
          <p class="text-body-sm text-muted-foreground">Fragen Sie auf Niederländisch, Französisch oder Englisch — finden Sie Bestimmungen in allen Amtssprachen Belgiens. Mit juristisch korrekter Terminologie in allen drei Sprachen.</p>
        </Card>
      </div>
      <div class="mt-10 max-w-4xl">
        <ScreenshotPlaceholder label="Screenshot: Dreisprachige Suche" suggestion="Suchergebnisse mit einer englischen Anfrage, die niederländisch- und französischsprachige Gesetzgebung zurückgibt, mit Sprachkennzeichnungen (NL/FR/EN) auf jeder Quelle und Übersetzungsschaltfläche." />
      </div>
    </Section>

    <Section>
      <div class="max-w-2xl mx-auto text-center">
        <h2 class="text-h2 font-bold mb-6">
          Treten Sie der Warteliste bei
        </h2>
        <p class="text-body-lg text-muted-foreground mb-8">
          Gehören Sie zu den Ersten, die KI-gestützte belgische Steuerrecherche
          erleben, entwickelt für Fachleute.
        </p>
        <a
          href={`/${locale}/waitlist`}
          class="inline-flex h-11 min-h-[44px] items-center justify-center rounded-lg bg-primary px-6 text-body-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
        >
          Zur Warteliste
        </a>
      </div>
    </Section>
  </main>
</BaseLayout>
