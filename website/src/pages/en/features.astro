---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Section from "@/components/layout/Section.astro";
import Card from "@/components/ui/Card.astro";
import ScreenshotPlaceholder from "@/components/ui/ScreenshotPlaceholder.astro";
import { t } from "@/i18n/utils";

const locale = "en";
---

<BaseLayout
  title={`${t("nav.features", locale)} — ${t("site.title", locale)}`}
  description={t("site.description", locale)}
  locale={locale}
  path="/features/"
>
  <main id="main-content">
    <Section>
      <div class="max-w-3xl">
        <p class="text-body-sm font-medium text-primary uppercase tracking-wider mb-4">Features</p>
        <h1 class="text-display font-bold tracking-tight mb-6">
          Research tools built for tax professionals, not general-purpose guessing.
        </h1>
        <p class="text-body-lg text-muted-foreground">
          Three pillars that set Auryth apart: confidence you can measure, reasoning you can follow, and Belgian tax depth no general AI can match.
        </p>
      </div>
    </Section>

    <Section muted id="honest-by-design">
      <div class="max-w-3xl mb-8">
        <h2 class="text-h2 font-bold text-foreground">{t("trust.transparency", locale)}</h2>
        <p class="mt-4 text-body text-muted-foreground">{t("trust.transparency_desc", locale)}</p>
      </div>
      <div class="grid gap-6 sm:grid-cols-2 max-w-4xl">
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Confidence Scores</h3>
          <p class="text-body-sm text-muted-foreground">Every answer scored for reliability. High, moderate, or low confidence — always visible, so you know exactly how much weight to give it.</p>
        </Card>
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Source Citations</h3>
          <p class="text-body-sm text-muted-foreground">Every claim linked to its legal source. Article references, rulings, and circulars — always verifiable, never a black box.</p>
        </Card>
      </div>
      <div class="mt-10 max-w-4xl">
        <ScreenshotPlaceholder label="Screenshot: Confidence score UI" suggestion="Annotated UI: 0-100 confidence meter with green/amber/red zones, source count badge, list of cited authorities with legislation/ruling icons, and temporal validity indicator." />
      </div>
    </Section>

    <Section id="no-black-box">
      <div class="max-w-3xl mb-8">
        <h2 class="text-h2 font-bold text-foreground">{t("trust.precision", locale)}</h2>
        <p class="mt-4 text-body text-muted-foreground">{t("trust.precision_desc", locale)}</p>
      </div>
      <div class="grid gap-6 sm:grid-cols-2 max-w-4xl">
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Reasoning Trail</h3>
          <p class="text-body-sm text-muted-foreground">Follow the AI's logic step by step. See which sources were consulted, how they connect, and why the answer was reached.</p>
        </Card>
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Open Methodology</h3>
          <p class="text-body-sm text-muted-foreground">Our retrieval system, scoring model, and source hierarchy are documented. No proprietary magic — just rigorous engineering you can inspect.</p>
          <a href={`/${locale}/how-it-works/`} class="mt-3 inline-block text-body-sm font-medium text-primary hover:underline">See how it works &rarr;</a>
        </Card>
      </div>
      <div class="mt-10 max-w-4xl">
        <ScreenshotPlaceholder label="Screenshot: Source panel" suggestion="Expanded source panel: article excerpt with highlighted relevant passage in yellow, publication date, authority tier badge (e.g. 'Tier 2 - Legislation'), and 'Open original' link." />
      </div>
    </Section>

    <Section muted id="jurisdiction-deep">
      <div class="max-w-3xl mb-8">
        <h2 class="text-h2 font-bold text-foreground">{t("trust.belgium", locale)}</h2>
        <p class="mt-4 text-body text-muted-foreground">{t("trust.belgium_desc", locale)}</p>
      </div>
      <div class="grid gap-6 sm:grid-cols-2 max-w-4xl">
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Belgian Source Hierarchy</h3>
          <p class="text-body-sm text-muted-foreground">Understands the Belgian legal hierarchy: federal law, regional decrees, circulars, rulings, and administrative commentary.</p>
          <a href={`/${locale}/how-it-works/#authority-hierarchy`} class="mt-3 inline-block text-body-sm font-medium text-primary hover:underline">See how we rank sources &rarr;</a>
        </Card>
        <Card>
          <h3 class="text-body-lg font-semibold text-foreground mb-2">Trilingual NL/FR/EN</h3>
          <p class="text-body-sm text-muted-foreground">Query in Dutch, French, or English — find provisions in any of Belgium's official languages. With legally correct terminology across all three.</p>
        </Card>
      </div>
      <div class="mt-10 max-w-4xl">
        <ScreenshotPlaceholder label="Screenshot: Trilingual search" suggestion="Search results showing an English query returning Dutch and French legislation, with language tags (NL/FR/EN) on each source, and translation toggle." />
      </div>
    </Section>

    <Section>
      <div class="max-w-2xl mx-auto text-center">
        <h2 class="text-h2 font-bold mb-6">
          Join the Waitlist
        </h2>
        <p class="text-body-lg text-muted-foreground mb-8">
          Be among the first to experience AI-powered Belgian tax research built
          for professionals.
        </p>
        <a
          href={`/${locale}/waitlist`}
          class="inline-flex h-11 min-h-[44px] items-center justify-center rounded-lg bg-primary px-6 text-body-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
        >
          Join Waitlist
        </a>
      </div>
    </Section>
  </main>
</BaseLayout>
